#!/usr/bin/env node
console.log('my first cli');

console.log(process.argv); //arguments that passes with the cli
///we can use Yargs to easy access the options in a key value pairs

//we can use inquire package to make the cli interactive
